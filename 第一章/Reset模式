Reset可搭配參數使用
	常見的有三個：
	--mixed:
		預設參數，會把“暫存區”檔案丟掉，不影響“工作區”檔案，拆出的檔案放		置於工作區
	--soft:
		暫存區與工作區檔案皆不影響，僅移動head-拆出的檔案放置於“暫存區”
	-hard:
		工作區與暫存區檔案全部清除

#重點：Reset並不會使任何commit消失，而是指向某個“版本”的狀態，例如先跳回前2版
	git reset HEAD~2
再透過Reset回到當前版本，則檔案沒有任何減少

